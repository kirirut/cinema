-- =============================================================================
-- Online Cinema — создание базы данных PostgreSQL
-- =============================================================================
-- Запуск от суперпользователя:
--   psql -U postgres -f init.sql
--
-- Скрипт идемпотентен: не создаёт БД повторно, если она уже существует.
-- После этого примените схему:
--   psql -U postgres -d cinema -f schema.sql
-- =============================================================================

SELECT 'CREATE DATABASE cinema ENCODING ''UTF8'''
WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'cinema')\gexec
